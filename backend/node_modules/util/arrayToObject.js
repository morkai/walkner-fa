// Part of <https://miracle.systems/p/walkner-fa> licensed under <CC BY-NC-SA 4.0>

'use strict';

module.exports = (keys, value, createValue) =>
{
  const object = {};

  keys.forEach((key, i) =>
  {
    object[key] = createValue ? createValue(key, i) : value;
  });

  return object;
};
